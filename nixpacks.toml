# Pin the Node version. distube, @discordjs/voice and undici all need 22.12+;
# without this Railway resolves Node 18 and the @distube/yt-dlp postinstall
# dies with "ReferenceError: File is not defined".
[phases.setup]
nixPkgs = ["nodejs_22"]

[start]
cmd = "npm start"
